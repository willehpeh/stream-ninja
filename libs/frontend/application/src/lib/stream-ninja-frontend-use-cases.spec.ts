import { streamNinjaFrontendUseCases } from './stream-ninja-frontend-use-cases';

describe('streamNinjaFrontendUseCases', () => {
  it('should work', () => {
    expect(streamNinjaFrontendUseCases()).toEqual(
      'stream-ninja-frontend-use-cases'
    );
  });
});
