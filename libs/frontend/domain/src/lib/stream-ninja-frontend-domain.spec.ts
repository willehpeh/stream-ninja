import { streamNinjaFrontendDomain } from './stream-ninja-frontend-domain';

describe('streamNinjaFrontendDomain', () => {
  it('should work', () => {
    expect(streamNinjaFrontendDomain()).toEqual('stream-ninja-frontend-domain');
  });
});
